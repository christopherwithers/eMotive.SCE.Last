@using eMotive.Models.Objects.Users
@model UserWithRoles
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
@section Crumbs
{
    			<ul class="breadcrumb">
			        <li>
			            <a href="@Url.Action("Index","Home", new {area = "Admin"})">Home</a> <span class="divider">/</span>
			        </li>
                <li>
					<a href="@Url.Action("Index","Users", new {area = "Admin"})">Users</a> <span class="divider">/</span>
				</li>
				<li class="active">
					Create User
				</li>
			</ul>
}
<div class="page-header">
    <h2>
        Create User
    </h2>
</div>
@Html.Partial("_modelStateError")
@using (Html.BeginForm("Create", "Users", new { area = "Admin" }, FormMethod.Post, new { @class = "form-horizontal" }))
{
    @Html.Partial("_userForm")
}